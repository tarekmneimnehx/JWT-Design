Pill for categories and portfolio filters. Static label by default; pass `onClick` to make it an interactive filter and `active` to mark the selected one.

```jsx
<Tag active count={24} onClick={fn}>All projects</Tag>
<Tag onClick={fn} count={8}>Residential</Tag>
<Tag solid>Dubai</Tag>
```
