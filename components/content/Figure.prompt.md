The base photography frame — every project image renders through it for consistent ratio, radius and captioning.

```jsx
<Figure src={url} alt="Living room" ratio="3 / 4" zoom
        caption="Al Barari Villa" meta="Dubai · 2024" />
```

`ratio` is any CSS aspect-ratio. `zoom` adds the slow hover scale used in galleries. `shadow` for floating hero images.
